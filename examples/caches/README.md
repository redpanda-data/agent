## Run

Run our cache services as well as our observability tools in the background:

```shell
docker-compose up -d
```

Then run our MCP server:

```bash
agent --address 0.0.0.0:4195 .
```

Then do some AI stuff or something?


